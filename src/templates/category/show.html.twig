{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    {% block header %} {{ parent() }} {% endblock %}
    <h1 class="h3">Category {{ category.name }}</h1>


    <a href="{{ path('category_index') }}">back to list</a>

{% endblock %}
