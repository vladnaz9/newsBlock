{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    {% block header %} {{ parent() }} {% endblock %}
    <div class="container">
    <h1 class="h3">Новости категории {{ category.name }}:</h1>

    {{ include('news/_list.html.twig') }}
    <a href="{{ path('category_index') }}">back to list</a>
    </div>
{% endblock %}
